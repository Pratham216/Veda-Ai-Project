# Base URL of the VedaAI backend (Render in prod, http://localhost:4000 locally).
# This is what the browser hits for REST + WebSocket — must be reachable from the user's browser.
NEXT_PUBLIC_API_BASE=https://your-api.onrender.com
